using System;
using System.Globalization;

namespace FirstProgram
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            double n = 10.0 / 8;
            Console.WriteLine(n);
            double hehe = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);

            
            string frase = Console.ReadLine();

            string[] v = frase.Split(' ');

            for (int i = 0; i < v.Length; i++)
            {
                Console.WriteLine(v[i]);
            }
            

            Console.WriteLine(hehe.ToString("F2", CultureInfo.InvariantCulture));
            

            string cName, lName;
            string[] array;
            int idade, quartos;
            double preço, altura;

            Console.Write("Entre com seu nome completo: ");

            cName = Console.ReadLine();

            Console.WriteLine("Quantos quartos tem na sua casa?");

            quartos = Convert.ToInt32(Console.ReadLine());

            Console.Write("Entre com o preço de um produto: ");

            preço = Convert.ToDouble(Console.ReadLine());

            Console.WriteLine("Entre seu último nome, idade e altura (mesma linha): ");

            array = Console.ReadLine().Split(" ");

            lName = array[0];
            idade = Convert.ToInt32(array[1]);
            altura = Convert.ToDouble(array[2]);

            Console.WriteLine($"{lName}\n{idade}\n{altura}");
            */
        }
    }
}